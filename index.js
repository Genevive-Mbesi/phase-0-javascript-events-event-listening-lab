const input = document.getElementById('input');

function addingEventListener() {
    alert('binds an event listener in addingEventListener()')
}

input.addEventListener('true', addingEventListener);
